---
doc: Program Backlog (Epics)
id: PM-004
version: 1.0
status: Living — ratified working register (the operative item register paired with PM-007 + PM-008)
created: 2026-06-18
updated: 2026-06-21
depends_on: PM-000, PM-007 (As-Built Spec), PM-008 (Release Governance), audit/02_GAP_ANALYSIS, audit/04_RISKS_DECISIONS_ROADMAP
---

# Program Backlog — Epics

The twelve gaps, the seven "keep" capabilities, and the net-new enterprise layer, converted into epics, mapped to phases and owning squads. Priority: **P0** (foundational/blocking) · **P1** · **P2**. Each epic links its gap (`audit/02`).

## Epic map

| Epic | Title | Gap | Phase | Squad | Pri | Acceptance (slice done) |
|---|---|:--:|:--:|---|:--:|---|
| **E-00** | Target Spec v1.0 supersedes both packages | — | 0 | Architect+Product | P0 | One current spec; ADRs renumbered canonically; decision log ratified |
| **E-01** | Validate as-built schema on real Postgres; kill SQLite-isms + no-op CHECK | — | 0 | Plat&Data | P0 | DDL migrates clean on PG15; roundtrip test green; `[D-6]` defects closed |
| **E-02** | Obtain & reconcile source-of-truth (repo, migrations, tests, ECLS) | — | 0 | Architect | P0 | DEP-1 closed; 63 tables/14 migrations/796 tests verified, not asserted |
| **E-03** | Multi-tenant `client` + RBAC/actor model | net-new | 0/A | Security | P0 | Tenant isolation enforced at row + API; roles defined; access tests pass |
| **E-04** | Security & NFR specification (PII, retention, threat model, sizing) | net-new | 0/A | Security | P0 | Signed NFR spec; data classification on every entity; perf targets set |
| **E-05** | Make the audit event log **live** (finish the hash-chain) | G11 | A | Security+Plat&Data | ✅ Closed v1.4 (G-B) | **✅ Closed v1.4 (G-B)** — decision events fire in-txn at ingest/seal/freeze/supersede/adjustment; hash-chained + tamper-evident (setup/capacity ingest emit no event — note in 07 §8) |
| **E-06** | KEEP-list hardening (identity FKs, calc-run spine, landed cost, eligibility, VSP) | KEEP | A | Plat&Data+Engine | P1 | Carried forward intact; covered by tests; no regression to thinner model |
| **E-07** | "Open last cycle" query + read model | — | A | Engine | P0 | Any cycle reopens with full story < 2s (S1) |
| **E-08** | iTrade **receipt-grain** feed (`itrade_receipt`) | G6 | B | Plat&Data | P0 | Real pull lands; flag-first validation; impossible-date-span rejection |
| **E-09** | KCMS scan feed (`kcms_movement`) | G6 | B | Plat&Data | P1 | Scan/margin lands; distinct from iTrade |
| **E-10** | Supplier scorecard — two frozen snapshots (derivation) | G6 | B | Plat&Data | P1 | Kickoff + sign-off snapshots compute from receipts |
| **E-11** | Persistent `norm.lot` + attribute taxonomy + sticky map | G8 | B | Plat&Data | P0 | Items propose lots; human confirms; map sticks across cycles; regroup by attribute |
| **E-12** | Two origins + `zip_centroid` distance | G7 | B | Plat&Data | P1 | `grow_origin`/`ship_from_zip` separate; distance derived (lift `calculate_distances`) |
| **E-13** | **REAL-DATA PILOT** (Phase B exit gate; retires R1) | — | B | QA+all | P0 | One real cycle end-to-end on real iTrade + bids; engine reproduces v3 (S2) |
| **E-14** | Kickoff keystone — rich `cyc.*` (objective, pricing+safeties, PBA, working capital, KPM, RFI, narrative) | G5 | C | Product+Plat&Data | P0 | A cycle declared from a real kickoff doc; structured drives system, prose stays prose |
| **E-15** | Pricing model + **safety TERMS** declared & stored at kickoff | G4 | C | Product+Plat&Data | P0 | `cyc.cycle_pricing` + `cyc.cycle_safety` store the declared contract terms/params; **the engine does NOT consume them** (D13/ADR-0014) |
| **E-29** | **Contract execution — safety reprice + market feed + reprice-and-layer** | G4 | E+ | Engine+Plat&Data | P1 | Formulaic safeties (rolling midpoint, tolerance band, collar) compute/visualize; disaster/inverse triggers flag for human reprice (revert-to-contract); every move lands in `awd.award_layer`; needs market-price feed (DEP-6) |
| **E-30** | **Kanban views — RFP portfolio + RFP-by-supplier** | net-new | F | Experience | P1 | Board 1: every RFP as a card across the process-rail stages (the cycle timeline, G10). Board 2 (drill-in): within one RFP, suppliers as cards across their lifecycle (invited → submitted → in-round → shortlisted → awarded/declined). Reads from the store; same render live or historic. Sponsor-requested. |
| **E-31** | **Gate-closure backup export (DR / historic)** | net-new | E | Engine+Experience+Security | P1 | At **every governance gate closure / decision point** (kickoff in-gate G12, each round close, award freeze G3, sign-off out-gate) generate a **downloadable portable snapshot** — full cycle state at that point (structured export, e.g. a zip of JSON/Excel + the generated docs) — the sponsor saves to the drives for historic / emergency recovery. Portable fallback the sponsor controls, on top of the live store + event log + freeze-and-layer. Also a trust/adoption lever. Sponsor-requested. |
| **E-32** | **AI assistant (read-only) — NL data recall + drafting** | net-new | F+ / post-MVP | Experience+Engine+Security | **P3 (wish-list)** | Read-only natural-language layer over the store: answer questions / recall data, and help **draft** emails/letters/summaries. **Never writes to the DB**; never auto-asserts an award (decision-support-only); reads are **RBAC/tenant-scoped** (only what the user may see); drafts pass the human + draft→sent gate (G9); answers are auditable. Built on the latest Claude models. **Lowest priority.** Sponsor wish-list. |
| **E-16** | Process rail generated from the cycle timeline | G10 | C | Experience+Engine | P1 | Console renders the rail from `cycle_timeline_event`, not hardcoded |
| **E-17** | Stage-0 governance **in-gate** | G12 | C | Security | P1 | A cycle cannot open on real data without the in-gate approval |
| **E-18** | v3 brain → `bid_score` 5-factor scoring + eligibility inputs | G2 | D | Engine | P0 | Reproduces v3 banded scoring (Price.35/Cov.25/Hist.20/Z.10/Cont.10) |
| **E-19** | Scenarios A–G (lenses) | G2 | D | Engine | P1 | Seven lenses produced; A = lowest-cost reference, never auto-applied |
| **E-20** | **Split allocation** — `scenario_award` + `volume_share` + cap-breach (ships with E-18) | G1 | D | Engine+Plat&Data | ✅ Operational | **✅ Operational** — engine ships split allocation (Scenario D, `volume_share`, `cap_breach`); cell awards to multiple suppliers, capacity-constrained, permit-not-force (S4) |
| **E-21** | Award object → freeze → `award_layer` | G3 | E | Engine+Plat&Data | P0 | Selection promoted to award; `frozen_at` seals; changes layer; raw recoverable (S6) |
| **E-22** | Portfolio sign-off gate (`signoff`) + savings-vs-STLY | G3 | E | Engine | P0 | Portfolio savings rolls to a sign-off total (S8) |
| **E-23** | Generated outputs — booking guide → deck → letters → email | G3 | E | Engine+Experience | P0 | Generated from records (lift v1.4 generators); booking guide first (S5) |
| **E-24** | Draft→**SENT** governance gate | G9 | E | Security+Engine | P1 | "Sent" = official, approver+timestamp; engine still never auto-asserts |
| **E-25** | REST API hardening (contract-first, authn/authz) | — | F | Engine+Security | P0 | OpenAPI complete; every endpoint guarded |
| **E-26** | Enterprise web app (stack D6) — the console | — | F | Experience | P0 | Live + historic cycles render identically from the store; ADR-001 honored |
| **E-27** | Platform: environments, CI/CD, IaC, observability | net-new | 0→F | DevOps | P0 | 3 envs; pipeline green; migrations automated; metrics/logs live |
| **E-28** | **Supplier behavior — contracted-vs-effective analytics** | net-new | E / post-pilot | Engine+Plat&Data | P1 | Per supplier×DC×lot: **contracted price vs iTrade actual-paid (effective) delta**, plus fill rate / rejection / on-time / supply-continuity from iTrade; merchandiser-facing flags ("priority supplier's effective rate ran above contract", "stopped supplying this DC", off-contract drift). Derivation over `perf.itrade_receipt` + `awd.award` + historical booking guides. Compounds from cycle 2. |
| **E-33** | **PBA / Contract builder — the post-award final step** | net-new | E | Product+Engine+Experience | P0 | The **last step after award**: build the executable **PBA / contract** from the frozen award. Pulls the kickoff-declared PBA + pricing + safety **terms** (E-14/E-15), the awarded supplier×DC×lot×price, volumes and effective dates from `awd.award` (+ layers, E-21) into a structured, reviewable, **versioned + auditable** contract document; draft→approve→**SENT** governance (G9/E-24); exportable. **Sponsor: spec out NOW and prep to go live while the RFPs run** (build in parallel with the pilot). |
| **E-34** | **Supplier master list — importer + per-RFP participant selection** | net-new | B/C | Plat&Data+Experience | P1 | A shared, growing supplier master (`ref.supplier`): an **importer** that ingests an exported supplier list, validates, and **upserts** (adds new rows, updates changed ones), idempotently. Plus per-RFP **selection of participating suppliers by the categories they sell**. Sponsor-described intake. (Groundwork landed: setup-ingest now treats DC + supplier as shared master data, reusing by natural key — D36.) |
| **E-35** | **Per-period price-movement / timeframe-discovery view** | net-new | E/F | Experience+Engine | P1 | Over the **flat-13 period storage** (D35/D38; G-A closed v1.6): study how prices move **individually (per period)** and **as a block (grouped)** over time, so buyers build timeframes empirically instead of guessing. Sponsor UX: a **table with period-over-period direction** (▲/▼/▬ + %) showing each supplier's curve **vs the block rollup side by side** — the supplier-vs-group *gap* is where a timeframe seam shows — highlighting stable runs as candidate timeframes; plus a **histogram** of price levels + move sizes (volatility/clustering). **Histogram controls (sponsor, 2026-06-21):** **DC and Lot filters** + **DC and Lot grouping**, so the period-movement / "timeframes" can be seen **by each DC and by each lot** (the seam where a timeframe breaks may differ per DC/lot). **Also (sponsor, 2026-06-21):** the same per-period movement / histogram view must be available **in the SETUP view over HISTORICAL PO data** (not only in-cycle bids) — so buyers discover timeframes from prior purchase history while scoping a cycle. *Data dependency:* per-period historical PO granularity comes from the iTrade receipt feed (`perf.itrade_receipt`, dormant) → depends on the **E-08** importer; the already-active `perf.historical_award_*` baseline gives award-grain history but not full per-PO movement. One pilot RFP collects at full per-period grain expressly to seed this. Requires period-grain storage (Option B). |
| **E-36** | **Progressive timeframe commitment + partial/split award + continuation RFP** | net-new | E+ | Engine+Plat&Data+Product | P1 | Treat **timeframes as the unit of negotiation/award**: (a) **commit** settled timeframes between rounds so later rounds carry them fixed and the engine games out only the open ones (a persistent Scenario-F-style override); (b) **partial/split award** — freeze only the *included* timeframes (awards already key by `awd.award_line.tf_id`, so it's a freeze filter, **no schema change**), splitting the excluded ones off; (c) **continuation / sub-RFP** — spawn a new cycle scoped to the split-off timeframe(s) (`parent_cycle_id`), **seeded from the last bids/award we already hold** for them, run as a full process. Lets the buyer find the right point to break the award and rebid the deferred timeframes later. Sponsor-described; builds on the timeframe-grain award + period storage. |
| **E-37** | **Supplier communications — template-merge email drafts across the lifecycle** | net-new | E/F | Experience+Engine+Product | P0 | The **outward communications layer** the sponsor flagged as missing: produce an **editable email DRAFT** at every supplier touchpoint. **MECHANISM — NOT AI:** fixed **structured templates with placeholders**; the system fills the gaps deterministically from governed data the platform already holds (mail-merge), the SAME reproducible/auditable generation pattern as the workbook outputs — no LLM, no free-form generation. **Touchpoints (6):** (1) **invite** to the RFP; (2) **RFP template send** (deliver the round's bid template); (3) **incomplete-bid notice** at bid upload — for suppliers who submitted **incomplete lines (partially filled, NOT blank / no-bid)**, itemizing exactly which fields are missing per line (from `bid.bid_line.incomplete_reason_code` / the completeness classification + quarantine) so they can complete + resubmit; (4) **round feedback** (per-supplier standing between rounds); (5) **award notification** (won / not-won per supplier); (6) **PBA / contract** transmittal. **Draft-only** — the buyer reviews/edits/sends; the system never auto-sends. Placeholders merge from `cyc.invited_supplier`, the generated template, the bid-line completeness, the sealed analysis/round standing, `awd.award` + layers, and (touchpoint 6) the PBA (E-33). Subsumes the "letters/email" tail of **E-23**; is the *content* side of the draft→**SENT** governance (**E-24**, SIGNED_OFF/SENT). Likely a `comms` domain (template registry + a merge engine + a draft record), per-touchpoint field-resolvers, a review/edit/send UI, and export (`.eml` / clipboard) — no live SMTP for v1. **DATA DEPENDENCIES (3 gaps found 2026-06-21):** (a) **timeline** for the invite (process start/end, est. rounds, est. award date) → `cyc.cycle_timeline_event` (modeled, must be populated at kickoff); (b) **incomplete-line capture** for touchpoint 3 — today incomplete lines are CLASSIFIED but neither persisted nor field-itemized (only `BID` lines persist), so the ingester must surface (dc, lot, empty-field list) per incomplete line; (c) **capacity** for the award/feedback context → **E-38**. Feedback/rejection data (market-low benchmark, premium %, coverage vs floor, eligibility) is all in the sealed `eng.bid_score`. **Governance note:** draft generation + the draft-REVIEW UI (Phase-1 / Live-Run eligible) is **SEPARATE** from send / **SENT** governance (later, tied to sign-off — E-24 / G-D); do not bundle the two. **Sponsor: core to full functionality.** |
| **E-38** | **Supplier capacity — ingest, allocation-vs-capacity control dashboard, custom-build cap flag** | net-new | D/E | Engine+Plat&Data+Experience | P0 | **Split into E-38a/b/c (below).** **⚠️ Governance split (`08`): the accuracy-core — (1) ingest + (3) cap flag + the workbook tab of (2) — is Category-B, APPROVED for the Phase-1 build; the IN-APP dashboard part of (2) is Category-C, DEFERRED to the Phase-4 review. Do not build the in-app dashboard now.** Suppliers state their **capacity** (`Max Weekly Cases` / `Max Total Cases` per supplier × DC × item × TF) on the owned template's **`Capacity` sheet** (the reference tomato format carries `Total Vol Offered` per cell). (1) **Ingest + persist** supplier capacity into the **EXISTING baseline tables** `bid.capacity_statement` + `bid.capacity_constraint` (per-scope `max_weekly_cases`/`max_period_cases`; As-Built §16) — wire/ORM-map them, do NOT create a new store. (2) A **control dashboard during analysis — both on file (workbook tab) and in the app** — per supplier: total stated capacity, **volume allocated so far** across a scenario's awarded cells (Σ `volume_share` × `cyc.cycle_projected_volume.projected_period_cases`), and **% utilized**. (3) A **capacity cap flag** — warn/over-allocation flag when a scenario (especially the **custom / Scenario-F build**) allocates a supplier **beyond their stated capacity** (the engine enforces `max_sup_dc` today but has **no per-supplier capacity ceiling**). Feeds the award/feedback/rejection comms (the "you're at/over capacity", coverage-context lines). Sponsor-described; the capacity field is in the samples (tomato/`Total Vol Offered`). |
| **E-38a** | **Capacity ingest + persist** — Capacity sheet → `bid.capacity_statement` + `bid.capacity_constraint` | net-new | D/E | Plat&Data | ✅ DONE | **✅ DONE** — capacity IS ingested + persisted into the **EXISTING baseline tables** `bid.capacity_statement`/`bid.capacity_constraint` (key-validated, with supersede on re-send); wired the existing tables, **not a new store**. Runs on strict + flexible intake. |
| **E-38b** | **Capacity-check evaluator + surface** — allocation-vs-capacity control (workbook control tab / read endpoint) | net-new | D/E | Engine+Experience | ✅ DONE | ✅ **DONE (workbook tab, c362f6c)** — G-G closed (workbook surface); the evaluator is wired into the alignment workbook's Capacity Check tab. Residual in-app read endpoint/screen = E-38c (deferred). The pure evaluator (`output/capacity_check.py`) exists + is tested but has zero call sites; surface it as a workbook control tab and/or read endpoint, computed vs the active `eng.analysis_scenario_award`. Includes the (3) cap flag. |
| **E-38c** | **In-app capacity dashboard** | net-new | F (Phase-4) | Experience | DEFERRED | **DEFERRED (Category C — Phase-4 review).** The IN-APP capacity dashboard; do not build now (per `08` governance split). |
| **E-39** | **Canonical formula registry — one "table of calcs", referenced everywhere** | net-new | D/E | Engine+Plat&Data | P0 | Every cross-layer calculation defined **ONCE** as a named, pure, tested function in `app/engine/formulas.py` (the engine purity layer, so any layer can import it) and **referenced everywhere** — so no view, draft, or workbook re-derives a value inline and drifts. Sponsor-directed after a comms bug where the draft layer recomputed price differently from the scorer. **Seeded (PR #18):** `construct_price` (§7) + `premium_vs_low` (§2.4). **Sweep DONE (audit v1.14):** unified the two `construct_price` definitions via `construct_price_from_parts` (engine + ingester); centralized the scoring ratios (`z_score`, `coverage_ratio`, `delta_vs_historical`), the spend/savings family (`awarded_cases`, `line_spend`, `savings_dollars`, `savings_fraction`), and the post-award/comms deltas (`premium_dollars`, `weekly_impact`, `price_delta`) — referenced across the scorer, ingester, scenario workbook + read layer, booking guide, award read/service + post-award doc, and comms drafts. Behavior-preserving (engine golden + alignment-workbook tests byte-identical); covered by `tests/engine/test_formulas.py`. Bare single-operation expressions with no logic (e.g. `delta_vs_a`) + the synthetic `_STLY_UPLIFT` proxy left inline by design. **Status: ✅ substantially delivered.** |

| **E-40** | **Decision-rationale capture — per-cell decision note + freeze note → audit trail** | net-new (G-B adj.) | E | Security+Engine+Experience | P1 | The **WHY** layer that pairs with the actor-fidelity work (#4 captured WHO). Capture the buyer's free-text reasoning at two points the first-draft design proposes: (a) a **per-award-cell decision note** ("reasoning for this cell's supplier choice"), saved per cell; (b) a **freeze note** at assertion ("why this scenario over the alternatives; conditions/exceptions/context for the audit trail"). Today only run-level `NOTES.md` exists; decision events record who/what/when but no rationale. **Governance sub-decision (resolve before building):** where the rationale lives — a dedicated `decision_note` store keyed to the award line / freeze, vs. `FROZEN`-event `metadata` — and whether it is inside the hash-chain (the envelope rule is "no commercial values in metadata"). Design captured in `project/design/first_draft/`. |
| **E-41** | **In-app alignment deep workbench (closes G-I)** | G-I | F | Experience+Engine | P1 | The in-app equivalent of the Excel alignment workbook the first-draft design targets: a **per-cell editable award matrix** (change supplier per cell), **live "Custom build" (Scenario-F) building** with recompute, the **cell drill-down** (5-factor scoring + "why this pick" + LOW/GATED flags), and the eight **diligence tabs** (Suppliers / Lowest-cost / Coverage / Scoring / Landed / Share / Incumbent / Negotiation). Today the web screen runs / compares / freezes; the deep per-cell workbench is Excel-only (gap **G-I**). **Category-C per `08` — Phase-4 review before build.** Extends E-26. Design captured in `project/design/first_draft/`. |

| **E-42** | **Stateless storage — DB is the system of record; deliverables render on request, uploads not persisted** | net-new | A (brought forward) | Plat&Data+Engine | ✅ DONE | ✅ **DONE (2026-06-21, ADR-0018, slices s0–s6)** — sponsor lifted the timeline and made it a hard pre-live-RFP requirement. The **web console** now writes **zero** files: uploads stream to ingest (`ingest_*_bytes`), deliverables render on request from the DB (deterministic, E-39; `app/pilot/deliverables.py`), run identity lives in `pilot.run` (migration 0019), the vault is decommissioned on the console path (`persist_outputs=False`/`db_runs=True`). 247 tests pass incl. `test_uploads_leave_no_file_on_disk` + `test_downloads_render_from_db_with_empty_outputs`. **MCP harness unchanged** (defaults; keeps its file vault) — see the oracle note below. **Residual for Cloud Run:** the raw-as-received flexible upload → object storage (GCS) if retained for audit (sponsor leans discard); E-31 gate-closure export → GCS. |
| **E-44** | **Pricing modality (Routing) + configurable cost breakdown + grain-by-surface** (D42 + D43) | net-new | D/E | Engine+Plat&Data+Experience | P0 | The price MODEL made buyer-configurable on **setup** and verified against the manual potato model (`project/triage/MANUAL_MODEL_FINDINGS.md`). Scoped into vertical slices at phase entry. **Slices:** **(a) Modality picker (= the manual's "Routing": FOB / DELIVERED / XDOC)** — chosen on setup, **per lot / per DC**; **governs price construction → scoring → supplier allocation → award**; an **allocation filter** constrained by the bid file (modalities offered) + the merch team's decision (human-confirmed, not a silent auto-gate). XDOC = FOB + a supplier-stated cross-dock surcharge (our `vegcool_surcharge`), via a named cross-dock — not a Kroger-second-leg split. **(b) Configurable cost-line catalog** — a **persistent catalog**; on setup the buyer **explicitly selects** which lines are active (the **system never guesses**); toggle on/off (e.g. **RPC**), **add new lines**. Each line: name · sign (+/−) · **grain** (period / timeframe / yearly) · **unit ($/case or %)** · (if %) a **base = a buyer-selected set of ONE OR MANY cost lines** + **apply-order**, all set PER LINE on setup. **Discounts** are just sign-− lines that **select which cost line(s) they reduce** (one or many; FOB default). **(c) Modality-aware construct-price evaluator** — `construct_price_from_parts` (E-39) evaluates a *configured* set (sums **and** percent-of-base, deterministic apply-order), not the fixed five; modality selects the legs. **(d) Config-driven template + ingest** — `template_schema.py`/`template_generator.py` columns driven by the active set; `bid_ingester.py`/`period_fanout.py` fan-in at each line's grain. **(e) EngineConfig + seal** — modality + active-cost-line set live in `EngineConfig`, sealed into the input manifest (C2) since they govern awards. **(f) Grain-by-surface display** — per-period in collection + record-creation + movement (E-35); **mixed-grain analysis breakdown** showing each component at its own grain (e.g. RPC@TF, FOB@TF, delivery@period), **horizontal/chronological layout** (period columns + per-period total-landed + a **toggleable timeframe-total** column; not stacked); **scored price/scenarios/awards stay timeframe-grain.** **Reconciliation findings:** **(A) ✅ RESOLVED** — a discount is a cost line (sign −) whose **base is a buyer-selected set of one-or-many cost lines, configured on setup**; unit `%` (preferred, grain-robust) or `$`; engine evaluates `−(pct × Σ selected base lines)`, adapter converts legacy `$`; **(B)** **RPC** as a first-class cost line, not folded into All-In; **(C)** manual is **horizon-grain** (rounds), our per-period (D42) is the E-35 enhancement. **Acceptance:** modality set per lot/DC on setup drives a verifiable per-cell award basis; the active cost-line set drives the template, ingest, and price identically (E-39, one definition); % and $ lines both compute deterministically; modality + cost set are sealed (changing either changes the seal — extend the C2 tests); analysis shows the mixed-grain breakdown + the timeframe-total toggle while the awarded number stays timeframe-grain; harness/app/manual reconcile on the same inputs. Cross-links **D42**, **D43**, **E-39** (formulas), **E-34** (supplier modality capability), **E-35** (per-period), **A1** (`DESIGN_REQUESTS.md` setup), **C2** (seal), `MANUAL_MODEL_FINDINGS.md`. |
| **E-43** | **Version savepoints + version picker + compare-versions + ROUND/FINAL decision marker** | net-new | C (deep-workbench) | Experience+Engine | P1 | Sponsor-described alignment-meeting workflow, all on the EXISTING dashboards/tables. (1) **Savepoint + carry-forward** — in a live meeting, "save this" locks the current live Scenario-F build as a **named version**; open a fresh working copy to keep experimenting; each new build **seeds from the last locked version**. (2) **Version picker** — jump between the live work and the locked snapshots (already partly designed: "Sealed analyses · prior versions read-only · View live version"; engine already seals each run as `version_seq`). (3) **Compare versions** — the SAME tables/dashboards, two-up, a **version selector per pane** (R1 v1 vs R1 v2, or live vs locked) — distinct from the existing scenario-comparison (lenses A–G *within* one analysis); reuses the same gather/render run per `analysis_run_id` (the E-42 deliverable registry already addresses data per-version). (4) **ROUND vs FINAL marker** on each locked decision (interim round checkpoint vs the final award) — smallest, most shippable slice; pairs with E-40 (decision rationale). (5) **Each locked version clearly LABELED** with its **alignment meeting + date** (date exists via `sealed_at`; the meeting label is new). Cross-links: **E-40** (decision capture), **E-41** (in-app deep workbench/G-I), **E-36** (progressive timeframe commitment / persistent Scenario-F carry-forward). |

## Cross-references

- Gaps → epics: G1→E-20 · G2→E-18/E-19 · G3→E-21/E-22/E-23 · G4→E-15 · G5→E-14 · G6→E-08/E-09/E-10 · G7→E-12 · G8→E-11 · G9→E-24 · G10→E-16 · G11→E-05 · G12→E-17 · KEEP→E-06 · net-new→E-03/E-04/E-27.
- Risk retirement: R1→E-13 · R2→E-18/E-20 (D2) · R3→E-01/E-02 (D1) · R5→E-15 · R7→E-03/E-04.

## Notes

- **E-18 and E-20 are a single increment** (Ed's "ship together" rule): scoring and split both touch the solver core.
- **E-28 is mostly a derivation, not new plumbing:** once awards, iTrade actuals, and booking guides are in the store, it is a read/analytics + merchandiser-reporting layer over data we already model. It rides on the D11 savings-baseline work and the scorecard (E-10). Historical booking guides (DEP-5) backfill prior **contracted** terms so contracted-vs-effective works even before a full cycle has run in-system.
- **Real baseline, not synthetic (E-28 + D11 + the alignment savings story):** the alignment screen's "vs prior year" today is a **synthetic STLY proxy** (incumbent × 1.04). We already persist the prior award's **contracted** price (`awd.award_line.frozen_price`, immutable/versioned); once **iTrade effective** actuals land (E-08, `perf.itrade_receipt`), swap the synthetic proxy for the **real prior contracted/effective baseline**. The same two prices seed **E-36**'s continuation-RFP carry-forward and **E-35**'s contracted-vs-effective-vs-new-bid overlay. (Sponsor noted: the awarded price is already saved on the next RFP; iTrade shows the effective price.)
- **Live test runs on the NEW design (sponsor, 2026-06-22).** The live RFP test runs on the **rebuilt design (E-26)**, not the current frontend — so **E-26 is on the critical path to go-live** (not a Phase-F-later item for the live test). The design deliverable that gates it (screens + visuals + **midpoints**) is `project/design/DESIGN_PACKAGE.md` (+ `DESIGN_REQUESTS.md` §A/§B/§C). Current-frontend tweaks (e.g. the C4 "Over capacity" rename) remain useful but are not the live surface.
- **LIVE-TEST SPEC FREEZE (sponsor, 2026-06-22; D44).** Requirement capture for the live path is **closed**. From now on, anything discussed is triaged against *"is it vital to executing the live test runs?"* — if not, it's a **one-line backlog entry** (default = backlog), and it enters live scope only via an explicit sponsor OK (change-control gate). **Vital** = needed to (a) run a live RFP end-to-end on the new design (E-26), (b) produce correct/governed/reproducible analysis, or (c) avoid a data-integrity/audit failure. By this litmus the live-test scope is essentially **E-26 + the existing working pipeline**; **E-44** and other enhancements are **not** in the first live test unless the sponsor flags a specific piece as vital. No more spec drift.
- **Reconciliation seams — the "in-between spaces" (sponsor, 2026-06-22; `project/RECONCILIATION_SEAMS.md`).** The mapping/reconciliation seams between grains & systems (lot/item ↔ iTrade SKU, supplier/DC identity, units/pack-size, columns ↔ fields, dates ↔ periods) are where real-data integration silently breaks — no single screen owns them. Standing watch-list. **Newly surfaced (were discussed, not recorded):** an **editable column mapper** (override the inferred mapping / resolve ambiguities — vs. confirm-only today; flexible-ingest enhancement) and **unit/pack-size reconciliation** (likely unmodeled). **Planned intake path:** a deterministic **known-template adapter** (Python parser: manual templates → our key-stamped owned template → STRICT ingest; never direct-to-DB) — cross-links E-08/E-11/E-28/E-34/E-35/E-39.
- **Price-component grain by surface (sponsor, 2026-06-22; D42 RATIFIED).** Refines the flat-13 period storage (D35/D38) with *which* components are collected per-period and *where* the period grain is shown. **Collection (fan-in):** **FOB by period + freight by period; all other costs yearly** (one cycle-level value — don't ask suppliers for the yearly costs 13×). **Storage:** flat-13 (FOB/freight land per period; yearly costs apply across periods). **Display = grain by surface — per-period on three surfaces, timeframe everywhere else:** per-period in (1) **collection**, (2) **record creation** (creating/entering the per-period records — the bid template + manual per-period entry; records are born at 13), and (3) the **period-movement / timeframe-discovery display (E-35)** + the **timeframe build/regroup UX**; the **analysis data tables, scenarios, and awards stay timeframe-grain** (the engine roll-up) — never per-period. These are **UX behaviors to establish** and they **track the ingestion fan-in/fan-out (D38):** collect/create/store per-period → roll up to timeframe for engine/tables → expand to per-period only for collection, record creation, and movement. **Build touch-points when next opened:** the bid template (`template_schema.py` — FOB/freight per-period columns + yearly-cost columns), ingest fan-out (`bid_ingester.py`), the engine's price construction across grains (`runner.py` roll-up), and the design grain-by-surface behaviors (`DESIGN_REQUESTS.md` §B6). The current model over-collects (fans every component to 13) — not wrong, just refine when the template is next touched. **Open UX gap — changing a timeframe build *while in analysis*:** re-grouping periods into different timeframes mid-analysis (rebuild the timeframe, re-roll the tables/scenarios live) is an E-36-adjacent behavior the period-movement/regroup UX must support; flagged for design, not yet a discrete story. Cross-links D35/D38/D42, E-35, E-36.
- **Pricing modality + configurable cost breakdown (sponsor, 2026-06-22; D43 RATIFIED, sub-decisions OPEN).** The basis of award is a buyer choice made **on setup (A1)**, and the price breakdown is no longer the hardcoded five. (1) **Modality picker — FOB / DELIVERED / XDOC** — chosen even with full data; **governs price construction → scoring → supplier selection → award** (sealed in `EngineConfig`, C2). (2) **Configurable cost breakdown** — toggle cost lines on/off (e.g. **RPCs**) and **add new lines**; the active set drives the template columns suppliers fill, ingest fan-in (at the D42 grain), and the construct-price evaluation. (3) **Absolute OR percent lines** — a line may be `$/case` or a **% of a base applied at build**, so `construct_price_from_parts` (E-39) must **evaluate** a configured set (sums + percent-of-base, deterministic apply-order) — not the current flat sum of fixed params. **Build touch-points:** `EngineConfig` (+ modality + active-cost-line config), `app/engine/formulas.py` (construct-price evaluator), `template_schema.py` + `template_generator.py` (config-driven columns), `bid_ingester.py` + `period_fanout.py` (fan-in the active set), the A1 strategy panel (modality picker + cost-line manager), C2 (seal coverage). **Sub-decisions RESOLVED 2026-06-22:** modality **scope = per lot / per DC** (a per-scope attribute); modality is an **allocation filter** (constrained by the bid file + the merch team's decision, human-confirmed); cost lines are a **persistent catalog, buyer-selected on setup, system never guesses** (each line: name · sign · grain · unit $/% · per-line %-base + apply-order); **mixed-grain analysis display** — show each cost component at its own grain, **horizontal/chronological** layout (period columns + per-period total-landed + a **toggleable timeframe-total** column; not stacked), while the **scored price/award stays timeframe-grain**. **VERIFIED vs the manual potato model (2026-06-22, `project/triage/MANUAL_MODEL_FINDINGS.md`):** modality = the manual's **"Routing"** (FOB vs Routed, tracked side by side); the cost catalog maps to our existing engine fields (FOB↔`fob`, Delivery Surcharge↔`delivery_surcharge`, **VegCool XDOCK Surcharge↔`vegcool_surcharge`** — XDOC is a per-case surcharge via a named cross-dock, **not** a Kroger-second-leg split, correcting the earlier provisional def). **3 reconciliation FINDINGS (kept separate):** (A) manual discounts are **%**, our engine subtracts them as **$** with no conversion — the known-template adapter / D43 percent-lines must convert; (B) **RPC** is a real `+/- $/case` line, not yet a distinct engine field — add as a D43 cost line; (C) manual is **horizon-grain** (rounds, not per-period-13) — our per-period collection (D42) is an enhancement for E-35. Current fixed model isn't wrong — refine when setup/strategy + the template are next opened. Cross-links D42 (grain), D43, E-39, E-34, A1, C2, `MANUAL_MODEL_FINDINGS.md`.
- **MCP harness = the live-run VERIFICATION ORACLE (sponsor, 2026-06-21).** After E-42 made the web console file-free, the MCP harness is **kept running as-is** (file vault, full engine + analysis-file generation) on purpose: it runs the calculations/analysis independently so the sponsor can **compare the harness output vs the manual work vs what the app produces** during the live RFPs. Because both runtimes share the same deterministic engine + canonical formulas (E-39) and the same generators, identical inputs → identical analysis, so any divergence is a real signal. Practical note: harness uses isolated per-run DBs, the console the shared DB, so feed the **same setup + bid files** to both to line up the compare. Do not "converge" or retire the harness without sponsor sign-off.
- Epics are phase-anchored but groomed into vertical-slice stories at phase entry (Ways of Working §1).
- Estimates are deliberately omitted until D1–D7 and DEP-1 are resolved (they swing the sizing materially).
- **UX/UI design (2026-06-21):** the **current handoff package is `project/design/handoff/`** (v2 — six screens + **`Handoff.dc.html`** design-system/handoff page; `HANDOFF_NOTES.md`). It **incorporates all feedback** (STLY MODELED badge, 7 lenses, scenario capacity-feasibility status, run-status strip, intake exception queue, finalize/close-run step, AssertModal + sortable columns) and is the **frontend implementation target for E-26**; point the dev at `Handoff.dc.html` first (Tailwind tokens, component states, non-happy-path, field reference, a11y, Lucide). The first draft + the full review of record live in `project/design/first_draft/` (`DESIGN_REVIEW.md`, superseded). **Fidelity note:** the finalize step writes a `CLOSED` audit event — **now added to our `EventType` enum and wired** (C5, As-Built v1.26: `finalize_run` + `POST /runs/{slug}/finalize` emit the governed `CLOSED` event); the front-end finalize UI folds into E-26. **Auditor round 2 (verified, `handoff/DESIGN_FEEDBACK_v2.md`): UI baseline LOCKED** — only three tweaks remain before implementation: (1) compact-width status-strip short labels (no clipping), (2) refresh the stale Awards error screenshot (**verified not a live defect** — `demand` is defined; export-lag), (3) make "Hash-chain current" drill through to the latest audit event (data already in Run Detail). Everything else → live-run backlog. Classification: Login / Dashboard / Run Detail / Bid Intake / Awards / the Alignment *shell* = **B restyle** (folds into E-26); the in-app deep workbench = **E-41 (C, G-I)**; decision-rationale capture = **E-40**. One correction to send the design team: **label "vs STLY" as synthetic / modeled** (it is the `_STLY_UPLIFT = 1.04` proxy, not a real feed — see the note above re E-08/E-28). **Auditor design review (verified, 2026-06-21, in `DESIGN_REVIEW.md`):** central verdict reinforces keeping the calm design ("escalating gravity only at exceptions"); freeze modal + post-award form are **already built** (do not rebuild); factual fix — lenses are **A–G (7)**, not A–F. Verified net-new additions (additive, data exists): **scenario-level capacity feasibility** (E-38 surface), a **persistent run-status strip** (Run · Analysis · Award · Audit → E-26), and an **intake exception queue** (Intake restyle + E-37 t3).
- **Finalize / "lock & close run" step — BACKEND BUILT (C5, 2026-06-22, As-Built v1.26):** the first-draft design ended at Freeze with **no terminal close-out action**; the **in-app close-out is now built on the console** — `PilotService.finalize_run` (service.py) + `POST /runs/{slug}/finalize` lock the run **CLOSED** (new governed `EventType.CLOSED`, entity = the cycle, in-txn) after an award is FROZEN, gated on a frozen award (409 if not), idempotent, and surface the **award notices (won) + rejection notices (not-won)** via the existing render-on-request generators (no file persisted, E-42). Closed state is **derived** from the CLOSED event (no migration). This **partially addresses** the design's finalize step — it is the governed close-out + the notices' availability; the remaining tail is the **send / draft→SENT** governance (**E-24**) + the explicit **sign-off out-gate** (G-D / **E-22**) + the **front-end finalize UI** (folds into **E-26**). The harness-only `close_run`/`purge_run` teardown is untouched. It composes E-22 + E-37 + E-24; the design pass + UI remain.
- **Parking lot — email-drafter UI (E-37), do NOT spec yet (sponsor, 2026-06-21):** a future design ask for the comms review / edit / send screen. Recorded so it is not lost; raise with design when E-37's comms review UI is scheduled. **Not to be spec'd now.**
